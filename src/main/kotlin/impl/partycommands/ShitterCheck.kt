package impl.partycommands

class ShitterCheck {
    /*"checks" wether a person is a shitter or not, just random answers
    execption for specific names
    - > regocike? more like shittercike
    */
}